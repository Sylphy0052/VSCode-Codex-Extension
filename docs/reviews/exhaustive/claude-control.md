# Claude制御プロトコルの精査

対象:`src/claude/control.ts`の全1,000行と`test/unit/claudeControl.test.ts`の全1,111行。全関数・callback・型・テスト準備・期待値を静的に読んだ。コメントの「実測」は過去の記録であり、今回の実行結果ではない。

## 関数・分岐・テストの対応

| 関数群                                                                                                              | 確認した経路                                                                                 | 対応テストの内容と不足                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `buildUserMessage`                                                                                                  | 添付既定値と添付あり、本文の前に画像、JSONL                                                  | テキストと画像1件、1行化を確認。改行を含む本文・複数添付はこのファイルでは未検査                                                                  |
| `buildControlRequest/buildControlResponse`                                                                          | 外側type/request_id、成功応答の入れ子、JSONL                                                 | requestの完全一致あり。responseの直接試験なし。StreamSession側は別台帳                                                                            |
| `readControlRequest`                                                                                                | 異なるtype、request_id空、request非object、正常                                              | 正常・他type・字段欠損を確認。requestが配列・null、subtype欠損の直接試験なし                                                                      |
| `readControlResponse`                                                                                               | type不一致、responseなし、id空、subtype=error、それ以外、空errorの既定値                     | 正常・明示error・payloadあり/なしを確認。未知/欠損subtypeもok=trueになる経路は未検査                                                              |
| `readCommandList/readAgentList`                                                                                     | 一覧なし/非配列/空、各項目非object/空名、重複、正常。最初の名前を残す                        | 一覧の型違い・空・重複・空名を確認。項目の型違い、説明型違いは直接試験なし                                                                        |
| `readCommandsChanged`                                                                                               | systemかつcommands_changedだけ受理                                                           | 正常・別subtype・別typeを確認                                                                                                                     |
| `readModelList/readEffortLevels`                                                                                    | 一覧なし、項目不正、空slug、表示名/説明既定、effort有無/配列/文字列/token、FastMode有無      | 通常、effortなし、空slug、FastMode三値、引数型の不正token、一覧欠損を確認。null項目、effort非配列・非文字列、説明既定の値そのものは直接検査しない |
| `buildSetModelRequest/buildSetPermissionModeRequest/buildSetEffortRequest`                                          | モデル、承認mode、apply_flag_settingsのeffortLevel                                           | 各要求の完全一致と末尾改行を確認。応答が効いたことまでは確認しない                                                                                |
| `buildRenameSessionRequest`                                                                                         | rename_sessionとtitle                                                                        | 直接試験なし。改名を永続化できるかは本builderの範囲外                                                                                             |
| `readCurrentPermissionMode/readFastModeState`                                                                       | 欠損・空、正常mode、Fast on/off/未知                                                         | modeあり/なし、Fast三値と未知はfalseを確認                                                                                                        |
| `buildContextUsageRequest/readContextUsage`                                                                         | 要求生成、body/totalTokens欠損、有限数判定、共通buildContextUsageへ委譲                      | 要求完全一致、合計36,342/上限1,000,000から96%、上限なし、body欠損を確認。負数・無限大は直接試験なし                                               |
| `buildRewindFilesRequest/readRewindFilesResult`とfilter                                                             | dry_run両値、封筒失敗、canRewind非true、正常、filesChanged配列/非配列、要素型の選別、数値    | preview、適用、canRewind=false、protocol error、非対応CLIを確認。payload欠損・不正要素・errorなしは未検査                                         |
| `buildRewindConversationRequest/readRewindConversationResult`                                                       | interrupt両値、封筒失敗、rewound厳密true、各文字列の空/非文字列、cli由来error                | 要求、正常prefill、success封筒内のfalse、error封筒を確認。payload欠損・文字列trueは未検査                                                         |
| `buildSideQuestionRequest`とhistory map                                                                             | history既定/空/あり、fallbackNoticeあり/なしをsnake_caseへ変換                               | 履歴省略と2件の履歴、fallback有無を完全一致で確認                                                                                                 |
| `readSideQuestionResult`                                                                                            | protocol失敗、本文欠損時のpayloadError有無、synthetic true/false/その他、fallback object有無 | 通常、fallbackあり、protocol失敗、本文欠損、payloadError、synthetic=trueを確認。空文字本文、synthetic型違い、fallback不正型・字段欠損は未検査     |
| `readControlRequestProgress`                                                                                        | type/subtypeの短絡、id空、数値/文字列正規化                                                  | started、api_retry全字段、他subtype、id欠損を確認。非system、未知status、不正数値は直接試験なし                                                   |
| `buildSessionCostRequest/readSessionCost`                                                                           | get_usageを生成しparseSessionCostへ委譲                                                      | 要求完全一致、正常費用と行数・取得時刻、undefined/空bodyを確認                                                                                    |
| `readExtraUsage`とtoAmount                                                                                          | 入れ子欠損、is_enabledの型、decimal_places有限整数かつ非負、換算、数値欠損、0補完            | 無効状態・有効/上限到達・桁数欠損・入れ子欠損を確認。桁数0/負数/小数/極大、各数値の不正型は未検査。極大桁数ではscaleがInfinityとなり0を作り得る   |
| `buildStopTaskRequest/buildMcpStatusRequest/buildGetSettingsRequest/buildReloadSkillsRequest/buildMcpToggleRequest` | 固定subtypeと引数名                                                                          | stop/status/settings/toggleの完全一致。toggleはfalseのみ。reloadの直接試験なし                                                                    |
| `readMcpServersList`                                                                                                | 一覧なし、空名、disabled、connectedのtools/version有無、未知/failedの理由                    | connected・failed・disabled・unknown・空名・一覧欠損を確認。connectedのtools/version欠損・不正型は直接試験なし                                    |
| `describeCanUseTool`                                                                                                | 空tool名、input既定、AskUserQuestion委譲、command/file/permissions、汎用入力要約             | Bash完全一致、Editのkind/detail、WebFetchのkind/titleを確認。空tool名・inputなし・長い要約はこのファイルでは未検査。質問専用テストは別台帳        |
| `buildCanUseToolResponse/defaultDenyControlResponse`                                                                | accept/acceptForSessionはallow、それ以外deny、未解釈は固定deny                               | accept・acceptForSession・decline・cancelを確認。defaultDenyの直接試験なし                                                                        |
| `summarizeInput/str/strOrUndefined/num/rec`                                                                         | 400文字超で省略、文字列型と空、数値型と有限、object/null/arrayの区別                         | 上の試験を通して間接検査。要約400/401境界、NaN/Infinity/配列を組み合わせた全境界は未検査                                                          |

全assertionは固定fixtureを処理した返値の比較で、外部CLIを起動しない。toEqualで省略されたundefined字段を実フィールド不在の保証と解釈しない。optional chainingを使う正例の期待値は、実結果がundefinedなら失敗するため、未返却を黙って通す形ではない。

`readControlResponse`の未知subtype成功扱いと数値換算の極端値は、防御上の未検査境界として残す。現CLIがその入力を返す事実は確認していない。既存の合成side question応答と成功判定の問題は[F14](../f14-side-questions-background.md)を参照する。
