# 手動テスト全手順の精査

`docs/manual-test.md`全3732行を読んだ。準備、設定変更、全ケースの操作と期待値、CLI版依存、移送済み試験の説明、記録様式、対象外を確認した。記載されたコマンド・テストは実行していない。過去の「実測済み」「自動化済み」は当時の記録であり、今回の実行結果ではない。

## EX-MANUAL-01[P2]:同じ機能に両立しない合格条件が残る

190行の対応表はClaudeの途中分岐を実装なし・不可とするが、U-29〜U-31と実装は途中分岐を扱う。192行はbtwをCodex専用とするがU-32〜U-33はClaude版を扱う。対応表だけから対象を選ぶと実装済み経路を落とす。

U-43aとU-46の3620行は回答を作業中のAIへ送らないことを期待する一方、U-45はautoSendの既定trueで自動送信することを期待する。`config.ts:288`も既定true。前者にautoSend:falseの準備がなく、正しい自動送信を不合格にする。W-Aのリロード後手動再実行も既定の自動再開を無効化しておらず、W-Oの期待と競合する。仕様変更に合わせ、設定・対象版ごとに合格条件を一意にする必要がある。

W-Kの2360/2383行はレビュー指摘・失敗でもマージすることを合格とする。これはEX-FORGE-04の回帰を固定する期待値である。設計§16.31(b)はIssue#847で停止へ変更済みだが、同節の検証段落にも古い「失敗してもmergeは進む」が残る。別の製品不具合として重複計上しない。

## EX-MANUAL-02[P2]:障害の準備が検査対象へ届かない

C-23の413行は存在しない実行ファイルをcodex.executableへ設定するが、実装が読むキーはcodex.executablePath。記載どおりでは解決失敗を作れない。C-48のCodex画面を閉じてSIGKILL回収を待つ手順も、通常の1パネル破棄では共有AppServerConnectionをdisposeしないため、その操作だけでは期待する終了経路へ入らない。Claude側L-48は画面ごとのプロセスであり同じ前提ではない。対象PID・接続所有者・発火条件を分ける必要がある。

## 確認した観点と不足

Codex/Claudeの開始・承認・履歴・再開/分岐・設定・画像・差分・問い合わせ・MCP/skills/hooks/plugins・アカウント・使用量・計画・圧縮・入出力・btw・進捗を確認した。ループの上限/停止/待機、履歴のscope/監視/操作、活動記録も読んだ。ワークフローの起動/依存/隔離/承認/生成/ホスト統合/CI/レビュー/計画変更/通知/質問/再開/program/衝突/チーム受け渡しについて、実ホストが要る範囲とフェイクで代替する範囲を確認した。

UX群の検索/送信キー/IME/ショートカット/Markdown/差分復元/通知/履歴/選択範囲/プリセット/メニュー/分岐/btw/描画/テーマ/密度と、セカンドオピニオンの材料/要約/停止/相談継続/自動送信/承認/設定、ゴール下書きの確認/編集競合/取消、Advisorの外部読取りも全文確認した。

U-29の部分失敗はタイミングをずらして試す方式で、失敗段の再現入力が固定されない。U-31は先頭分岐の成功とエラーの両方を許容するため、探索記録には使えても先頭分岐の合格判定にはならない。ユーザーの途中分岐エラーをこの文書だけで特定したとは扱わない。[分岐精査](fork.md)とF03の条件を参照する。

C-48/L-48のバッファ側は正常な大出力を試すだけで、上限超過・改行なしの拒否経路の実機確認にはならない。U-23のEscapeは閉じることだけを見ており、応答中に不要なinterruptを送らないことを検査しない(EX-CHATUI-01)。U-12/U-13は操作前の変更や固定symlinkを扱うが、確認待ちの差替えと未保存エディタの内容保護はF09の不足として残る。

W-Sは両親ともマージブロック済みを前提とし、片親だけがブロック・他方が未完了のEX-RUNSTATE-01を覆わない。W-M/W-Oは未回答の質問の復元を扱うが、回答済み回数の累積上限復元(EX-ORCH-02)を検査しない。W-Bのallow確認は待機中のYAML変更(EX-EXT-01)を扱わない。W-Jの少数コメントはページ欠落(EX-FORGE-05)を検査しない。

U-43aのログ非露出という期待は下位CLIトレースまで含めて確認が必要(F39)。U-43/U-44のスナップショットと要約の期待は秘密情報の伏字、取得失敗による部分材料、確認中の別相談への入替えを検査しない。モデル出力・実行時間・描画の「自然」「目立たない」等は実機観察の基準であり、静的精査で合格とは判定できない。外部CLIの将来版への互換性と実ホスト操作も未確認のままとする。
