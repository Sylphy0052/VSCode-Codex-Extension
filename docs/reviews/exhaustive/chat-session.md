# Codex会話セッションの精査

chatSessionの全854行と下表のテスト本文を読んだ。テストは実行していない。chatState本体の精査は別記録とする。

| 対象                           | 関数・分岐・期待値と不足                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| chatSession:開始・復帰         | constructor、getState、update、start、resume、deferResume、loadForked、applyThreadSnapshot、resumeFailed、readThreadId/Name/InitialItemsを確認。未開始・不正応答・任意cwd/model/config、復帰中と失敗、名前fallback、turn/item配列の不正要素を読む。snapshotは外側turn.idを捨てる既存F03-08。進行中snapshotからbusy/turnIdを復元しない。dispose後の開始・復帰完了は既存F03-04。                                                                                                                                                                                   |
| chatSession:送信・操作         | noteSecondOpinion/LocalEvent、setPlanModeの同値・baseline未取得、sendの画像/本文・モデル/effort・承認・Plan/bypass/復帰policyの優先、steerのthread/turn不足、sendOrQueue、cancelQueued、popLastQueuedForInputの空/添付、flushQueue/sendNextQueued/sendQueuedの空・経路・復元catch、setName、compact、recap、startReviewのinline/detachedと不正応答を確認。send失敗のbusy残留F05-01、queue経路でもstartするF05-02、compact失敗F13-02は既存。policyOverriddenは送信成功前に更新し、後続失敗で戻さない。名前変更とreview失敗もawait前後の世代照合がない。           |
| chatSession:中断・承認         | interruptの未開始/turnなし/同一turn待機中/別turn、成功・失敗・finally、applyNotification、rememberDeniedReview、approveDeniedReview、requestApproval、dropResolvedApproval、decide、answerPrompt、releasePendingApprovals、markTurnFailed、dispose、messageを確認。中断応答の旧turnによる上書きはF05-03。resolvedは承認だけ扱いpromptを残すF10-08。同じ要求IDの再登録はresolverを上書きする。answerPromptはbuildPromptResponse前にMapから削除するため、構築例外時は解放対象からも消える。updateコールバック例外は伝播する。dispose後の新規要求を拒む状態はない。 |
| chatSessionRecap.test          | 未開始reject、最初の空会話は通知のみ、user itemありで指示文を通常送信、余分な通知なしを確認。2回目の空会話、ローカル通知しかない会話、busy、RPC失敗はない。                                                                                                                                                                                                                                                                                                                                                                                                      |
| chatReview.test                | 4形のreview結果ではなく、主にinline/detachedの送信形、戻りID、busy差、未開始、不正ID時のbusy解除を確認。RPC reject、旧review失敗と新turnの競合、元からbusyのdetachedはない。                                                                                                                                                                                                                                                                                                                                                                                     |
| chatSideQuestion.test          | loadForkedのID・非空items、不正応答throw、別セッションの状態保持を確認。itemsの正確な個数・本文・turnIdを検査しない。requestはthrowするフェイクだがensureStartedは無操作で、通信しないという名前の全範囲は保証しない。                                                                                                                                                                                                                                                                                                                                           |
| chatSessionSendOrQueue.test    | idle送信、busyの保留とRPCなし、FIFO、末尾の取出し、空と添付の拒否を確認。最後の「自動デキュー直後」はturn完了を通知せずsendNextQueuedを呼ぶためsteer経路であり、自動送信の完了時競合を再現しない。送信失敗・復元・本文空は不足。                                                                                                                                                                                                                                                                                                                                 |
| chatQueue.test                 | routeのstart/steer/queue、enqueueの空入力・添付・FIFO・元配列保持、先頭/末尾取出し、removeの中央と上下範囲外、clearを確認。takeQueuedAt/restoreQueued、非整数/NaNのindex、添付参照の変更は直接検査しない。                                                                                                                                                                                                                                                                                                                                                       |
| chatSessionInterrupt.test      | 正常要求形、busy/turnId、実行中commandの印と注記、遅延item更新でも印保持、連打時の注記/要求1回、失敗時のbusy解除・turnId保持、別turnへの要求、turnなし、完了seqをcompletedだけで進めることを確認。別turnのテストは送信要求だけを検査し、旧応答が新turnを消す状態は検査しない。                                                                                                                                                                                                                                                                                   |
| chatSessionConnectionLost.test | 承認cancelとprompt空回答を具体値で検査し、2セッションを個別解放、releaseでは拒否履歴保持、disposeでは履歴削除、送信応答保留中のmarkTurnFailed、idle前後と二重失敗のseqを確認。接続断自体は手動メソッド呼出しで再現し、実接続resetのpending rejectや再接続は扱わない。無更新の期待はtoEqualであり、onChangeの呼出し回数を検査していない。                                                                                                                                                                                                                         |
| chatApprovalsReviewer.test     | start/turnへのuser・auto_review、空/未知値の省略、Plan時省略、拒否の覆しに通知eventそのまま返却、未知IDと承認済み通知の無要求を確認。resume、timedOut、覆しRPC失敗・二重クリック・同IDの状態遷移はない。                                                                                                                                                                                                                                                                                                                                                         |
| chatBypassSandbox.test         | startで承認関連を省略、turnでexternalSandbox+never、競合設定より優先、PlanでreadOnly優先を確認。bypassを解除した次turnの復帰は検査しない。既存F10-02と対応。                                                                                                                                                                                                                                                                                                                                                                                                     |
| fakeAppServerConnection        | request番号・waiting登録、resolve/reject/Firstの探索と不在throw、通知・server request委譲、factoryの生成前throwを確認。disposeはwaitingを消すだけでPromiseをsettleしない。simulateDisconnectもコールバックだけでpendingをrejectしないため、実接続断との違いを考慮する。                                                                                                                                                                                                                                                                                          |

## EX-RECAP-01[P2]:空会話の2回目の要約がモデルへ送られる

[chatSession.ts:511](../../../src/appserver/chatSession.ts#L511)はitems.lengthだけで会話の有無を判定し、空ならappendNoticeで同じitemsへ通知を追加する。入力しないまま再度recapするとitemsが1件あるためsendへ進み、要約対象がない通常ターンを開始する。設定変更などのローカル通知だけがある場合も同じ。ユーザー/アシスタントの会話内容が存在するかを判定する必要がある。既存テストは初回の空会話だけで、通知追加後の再呼出しを検査しない。実機未確認。
