# Provider補助の精査

以下16ファイルを全文精査。テスト未実行。

| 実装と対応テスト                                                       | 関数・分岐・期待値の確認                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/codex/argvBuilder.ts`、`test/unit/argvBuilder.test.ts`            | UUID正規表現、危険設定の委譲と説明文を確認。bypass優先、sandbox通常、danger+never、danger+auto-review、残りの分岐。テストはUUID・不正id・各組合せ・説明文・優先順位を確認する。大文字UUIDなどの境界値はない                                                                                                                                                                                                              |
| `src/claude/argvBuilder.ts`、`test/unit/claudeArgvBuilder.test.ts`     | effort/permission/agentの検証、new/resume/forkの引数生成、無効UUIDのthrow、設定追加と警告、追加引数の型、危険フラグ2種を確認。テストはnew固定引数・resume・危険設定の一部。fork、無効UUID、設定の警告、agent/effort、不正追加引数、もう一方のskipフラグを検査しない。permissionのパラメーター試験はstdioフラグしかassertしないため、入力manualの警告化を判別しない。additionalArgsは設定画面から直接指定する項目ではない |
| `src/codex/accountActions.ts`、`test/unit/codexAccountActions.test.ts` | constructor、logout、APIキーをstdinへ渡すloginを確認。FakeRunnerの既定値・記録と成功/失敗返却を確認。キーが引数にないことはassertする。path取得例外・runner reject・実プロセスは未検査                                                                                                                                                                                                                                   |
| `src/claude/authActions.ts`、`test/unit/claudeAuthActions.test.ts`     | constructor、logoutの委譲、FakeRunnerの記録、成功/失敗を確認。起動失敗・path getter例外は未検査                                                                                                                                                                                                                                                                                                                          |
| `src/claude/settingsJson.ts`、`test/unit/claudeSettingsJson.test.ts`   | JSON parse例外、object/null/array判定、非空文字列判定、model/effort/permission抽出を確認。テストは正常・欠落・型不正・壊れたJSON・permissionsの型不正。root配列/null、空白のみの値はない。既定値は共有オブジェクトだが、この範囲に変更する呼出しはない                                                                                                                                                                   |
| `src/claude/sessionNames.ts`、`test/unit/claudeSessionNames.test.ts`   | Memento既定値、getの欠落/空白判定、setのread-modify-writeを確認。FakeMementoは同期Map更新。テストは欠落・保存・2件の逐次保存・空白・保存先省略。保存拒否、型の壊れた保存値、並行更新は扱わない。実Mementoの可視化時点を確認せず競合不具合とは断定しない                                                                                                                                                                  |
| `src/claude/transcriptWatcher.ts`                                      | constructorのcreate/change/deleteイベント転送、disposeの全解除と配列初期化を確認。専用テストは対象一覧にない                                                                                                                                                                                                                                                                                                             |
| `src/codex/skillDisable.ts`                                            | 関数なし。skills.include_instructions=falseの固定値と型・説明を確認。コメントに記載された過去CLIでの実験は今回の実行結果ではない                                                                                                                                                                                                                                                                                         |
| `src/codex/skillsList.ts`、`test/unit/skillsList.test.ts`              | object判定、data/skills配列、nameの型/空文字列、enabled=false除外、description既定値、複数cwdを確認。テストは有効スキル、説明、複数cwd、enabled省略、不正構造、name欠落。重複名・空名・description型不正などは未検査                                                                                                                                                                                                     |

この範囲で新規の確定不具合は追加していない。テストが通ることと、上記未検査経路の正しさは別である。
