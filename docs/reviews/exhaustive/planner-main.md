# 計画生成・単発ターン・レビューと全テストの精査

`src/orchestrator/planner.ts`全2137行と`test/unit/planner.test.ts`全1483行を読んだ。単発ターンの追加試験は既存の関連記録も参照。実行はしていない。

| 範囲                      | 確認した処理・分岐と試験の内容                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| workspace・スキーマ・指示 | 3並列読取り、失敗時の空情報、40件/100文字、隠し項目除外、provider/team/unattended、定数由来の列挙、ロードマップあり/なし、ゴール8000文字、同一nonceの外部本文区切りを確認。テストは構成・有無・改行制御文字・個別名上限・役割/モデル・生成provider・無人設定を確認。40件境界と既定fs失敗の直接試験はない。プロンプトの存在検査はモデルの遵守を保証しない。                                                                                                     |
| 抽出・生成                | 全フェンスの言語/空タグ、複数候補の20件上限、パース例外/容量/空tasks、全滅先頭fallback、素のルートキー、未依存参照修復、provider補完、初回成功/再生成成功/2回失敗を確認。試験はbash先行、無タグ、候補全滅、25件、巨大応答、参照除去、明示provider維持を確認。再生成は新セッションで、元ゴール/スキーマではなく失敗YAMLとエラーだけを送る。同一セッションというbuildRetryPromptのコメントとは違う。                                                             |
| 警告・名前                | provider別のクランプ比較、autoApprove/allow、行位置探索とtask境界/fallback、危険名・予約名・1000文字のパス走査・40/80文字・連番を確認。試験は通常4警告、警告なし、行番号、未発見、名前/区切り/日本語/予約名/長さ/ReDoS計時と境界跨ぎを確認。予約名の拡張子付き形式、slugのdot-only、補助平面文字の切断、case-insensitiveの既存名は直接試験しない。                                                                                                             |
| 単発ターン                | 起動前の固定権限検査、事前abort、起動await、初期化/承認拒否/open、dispose有無、部分回答、abort/timeout/finished/runLoop同期例外の先着決着、遅延通知を確認。timeoutはopenTaskSession待機には掛からない。manual/interruptedもfailed以外なので本文を成功応答として返す。共有セッションでの過去本文再利用の限界は呼出し側の寿命に依存する。テストは固定権限、拒否、1ターン、dispose、fake timerの停止と遅延応答、本文fallbackを確認。実CLI停止の完了は保証しない。 |
| 計画レビュー・修正        | 9観点、60000文字、JSONフェンス抽出、構文/配列/容量、項目の型・空message・未知aspect、30件、表示無害化、修正後の再パースと警告を確認。試験は正常/空/不正JSON/非配列/不正項目/上限/起動失敗/権限/nonce/指示を確認。「4観点」のテストは4つの存在だけを見ており、現在の9観点全てを検査しない。reviseWorkflowPlanはこの試験ファイルの直接対象外。                                                                                                                   |
| タスクレビュー            | prompt/done/diff、60000文字、JSON構文・配列、項目型、30件、失敗と空成功を確認。試験は通常/フェンス/壊れたJSON/message不正/件数/起動失敗/固定権限/nonce/本文を確認。「マージは止めない」というテスト名は現在の呼出し側と不一致で、このテストはマージそのものを確認しない。                                                                                                                                                                                      |

## F26-01の影響範囲の追加

`parseTaskPullRequestReviewFindings`も不正な配列要素を全て捨てると`findings: [], error: undefined`を返す。例:`[{}]`。`runner.ts:4073`以降の統合レビューと`runner.ts:4765`以降のsemantic検証がこれを成功扱いする。計画のready化だけでなく、独立検証・統合レビューの関門にも同じ欠陥がある。根本原因が同じためF26-01へ関連付ける。テストの「messageが空文字・欠落の項目は捨てる」は空findingsだけを期待し、errorを要求しない。

## EX-PLANNER-01[P2]:切り詰めた計画・差分を全体レビューの成功として扱う

`planner.ts:1653`付近の計画レビュー、`planner.ts:2000`付近のタスクレビューは60000文字で本文を切り詰めるが、戻り値に未確認範囲を残さない。`extension.ts:2588`は空指摘なら全定義をreadyへ変更し、`runner.ts:4090`付近は統合全体のレビュー通過として扱う。YAMLの受付上限は1MiBなので、受付可能な後半タスクがレビュー入力に入らなくても通過し得る。修正プロンプトも同じ上限で、末尾のタスクを保持する材料が失われる。

修正候補:入力全量を読める材料へ切り替えるか、分割して全範囲の結果を集約する。切詰めが残る場合は未確認として通過を止める。必要な確認は上限直前/直後のタスク・diffと、省略範囲の問題が成功に化けないこと。現在の試験は本文を短い固定値で与え、全範囲のレビュー成立を確認しない。モデルが実際に見逃した結果の再現試験は未実行。
