# ループ制御本体の精査

src/loop/loopController.ts全1,010行とtest/unit/loopController.test.ts全2,220行を精査した。状態遷移、関数・コールバック、各早期return、非同期処理の世代、全テストの入力と期待値を照合した。実行検証はしていない。

| 対象                 | 確認内容と不足                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 計画の正規化         | 非object/null、文字列trim、数値/文字列の回数、非有限/1未満拒否、切捨て/200回上限、外部設定のみのengineering/evaluator/advisor、goal成立条件、通常ループへの退避、duration未設定/無効/上限を確認。テストは主要経路を扱うが、maxIterationsの小数・非有限・末尾文字、duration数値0.5と文字列0.5の違いは扱わない。前者は正数判定後に切り捨てて0ms、後者はparseInt後に無制限となる。画面からの到達可否は画面精査へ残す。                                                                          |
| 開始・停止・一時停止 | 開始時の既存message/確定コマンドをbaseline化、世代更新、ledgerと判定待ちの初期化、初回指示代用、stopのboolean・no-op・abort・通知、pauseのno-opと保留、resumeの保留有無/期限再確認を確認。テストは再開前の各停止理由、走行中/評価待ちのpause-resume、停止後の古い評価/送信失敗、同一plan再利用を確認する。start自体は旧runAbortをabortせずpausedも消さないため、stopを挟まない開始し直しは旧補助AIとpauseを持ち越す。両viewのloop/startは直接startを呼ぶ。通常UIで再開始できるかは未確定。   |
| observeの入口と合図  | running/plan、busy、approval、queue、evaluating、sawBusy、完了世代の各ガード、message参照境界、失敗→撤退→通常完了の順を確認。テストはidle通知先行、確定後の二重送信防止、古い合図、同じidの新しいオブジェクト、ツールだけのターン、初回baselineを扱う。runTurnは毎回初期seqから0→1を作るため、実際の累積seqと別ターン通知の混在は検証しない。完了通知が別turnIdでもseqを進める状態層の問題はEX-STATE-01を参照。                                                                              |
| 継続・期限・停滞     | signature更新、停滞→時間→回数、pause時に判定済みpromptを保留、resume時の期限再確認、engineeringを終了条件より前へ連結、同期throw/Promise rejectでfailed、旧世代のreject破棄を確認。テストは停滞の空応答除外・閾値・履歴初期化、時間の前後1ms・hard timeoutでない仕様、終了条件の完全一致を確認。onStatusはsendのtry外で、同期再入・例外は直接検証しない。                                                                                                                                    |
| ゴール評価           | 新規確定コマンドのみ収集、当該turnResultTextの申告、同一inputでadvisor先行起動、双方のreject退避、世代照合、advisor連続失敗無効化、note例外遮断、blockerとachievedのconflicted、終局評価を停滞より優先、判定不能連続上限と解除を確認。テストは各verdict、指摘各severity、片側失敗、呼出間隔、指摘の非持越し、打切りと再開始、停止時abortを扱う。maxIndeterminateの非有限値、関数呼出自体の同期throw、noteによる再入、旧advisorが返る前の再開始は直接確認しない。長い証拠の欠落はEX-GOAL-01。 |
| テスト補助・待機     | stateはinitialChatStateの浅いコピー、agentMessage/commandItemは固定または明示id、spy配列、finishedのseq加算、runTurn、goalPlan、recordingEvaluator、deferredのrelease/reject、3回/8回のPromise待機とsetTimeout(0)を確認。今回は共有stateを直接変更しない。固定マイクロタスク回数は待機対象の実装構造に依存する。未解決のダミーPromiseを残す例はプロセス起動・タイマーを伴わない。                                                                                                            |

## EX-TEST-08[P2]:ゴール駆動で完了合図を無視するテストが有効な合図を渡していない

loopController.test.ts:999の「Workerの自己申告では止まらない」は、同じ最終行に「できました」と合図を並べる。declaresDoneは最終非空行の完全一致を要求するため、この入力は通常ループでも完了合図にならない。goalの除外条件を誤って消しても、このテストは止まらないままで回帰を検出できない。

合図だけの行、または本文と改行して合図を渡し、Evaluatorがcontinueを返した後もrunningであることと次の送信を確認する必要がある。現実装にはgoal除外があり、製品不具合としては扱わない。

「撤退の申告と終了条件が同時に成立」のテストも、DONEの次行にESCALATEを置くので最終行条件を両方が満たす入力ではない。実際に確認するのは撤退の認識であり、両述語の競合の証明にはならない。

## 制約

テスト、lint、型チェック、実機イベントの再生は実行していない。状態層、CLI、設定やWebviewを含む統合動作の保証ではない。
