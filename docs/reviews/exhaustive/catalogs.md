# 一覧・認証・CLI問い合わせの精査

対象ファイルの全文を読み、関数、早期終了、既定値、重複排除、例外、コールバックとテスト本文を照合した。実行検証はしていない。以下の「不足」はこの表のテストで確認できない分岐を示す。他のテストとの対応は全件精査で補う。

## 一覧の変換

| 実装とテスト                                             | 読んだ内容・不足                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `codex/mcpStatus.ts`、`codexMcpStatus.test.ts`           | 前の記録で実装を精査済み。今回はテスト全文を追加。接続状態、設定による無効化、名前順、壊れたルート、名前欠落を検査する。設定だけに存在するサーバー、重複名、toolsの非object、version欠落の組合せは不足。                                                                                                                                                                                                                                                           |
| `codex/skillsStatus.ts`、`codexSkillsStatus.test.ts`     | data/entry/error/skillの型ガード、必須name/path、pathによる先勝ち、name/key順、scope変換、enabled既定値。正常値、4origin、無効化、警告、整列、欠落、重複、未知scope、壊れたルートを検査する。同名のpath順、警告のpath欠落、nullの中間要素、継承プロパティ名のscopeは不足。                                                                                                                                                                                         |
| `claude/skillsList.ts`、`claudeSkillsList.test.ts`       | skills配列の有無、nameによる先勝ち、descriptionの接尾辞や名前のコロンによるorigin推定、reload応答の成功・エラー・壊れた形。テストはuser/project/plugin/unknown、固定のenabled/toggleable、重複、name欠落、ルート不正、空配列を検査する。`buildSkillsSnapshot`の直接検査、plugin名とdescription接頭辞が一致しない場合は不足。                                                                                                                                       |
| `provider/plugins.ts`、`pluginsProvider.test.ts`         | 型契約と文字単位の名前検査。正常名、空白、空文字、型違いを検査する。先頭ハイフン、DEL/C1、Unicode空白は拒否されず、テストもない。CLI側での扱いは未検証。`emptyPluginProvides`は共有の可変objectであり、呼出側の破壊的変更に注意が必要。                                                                                                                                                                                                                            |
| `provider/hooks.ts`、`hooksProvider.test.ts`             | 型契約、空文字・ASCII空白/制御文字・二重引用符・逆斜線の拒否。正常値、escape、空白/tab/newline、型違いを検査する。DEL以降の制御文字、Unicode空白は対象外。                                                                                                                                                                                                                                                                                                         |
| `claude/hooksSettings.ts`、`claudeHooksSettings.test.ts` | effectiveとsourcesの型ガード、group変換、source候補の消費、再帰的な比較、unknownへの退避、固定の有効状態・trust非対応。テストはcommand型の正常値、user/project、trust、未一致、hooks欠落・空を検査する。重複groupの消費、壊れた中間要素、配列長・キー違い、prompt/agent型は不足。比較前にtype/command/matcher以外を捨てるため、別のprompt等を持つgroupが同一視され得る。実際のorigin誤表示は未確認。                                                               |
| `codex/hooksStatus.ts`、`codexHooksStatus.test.ts`       | warnings/errors、必須key、重複排除、event/key順、origin/trustの既知集合、handlerとenabledの既定値、trust編集用の引用付きkeyPath。テストは正常値、plugin、警告、event順、欠落・重複・不正形、未知値、編集正常・入力拒否を検査する。全enum、同一event内の順序、既定handler、欠落hash、pathなしerrorは不足。                                                                                                                                                          |
| `claude/pluginsList.ts`、`claudePluginsList.test.ts`     | JSON例外、配列と要素、idによる先勝ち、marketplace分割、scope表示、enabled、name/key順、詳細テキストの行走査と件数抽出。テストは名前・origin・有効状態・操作可否・順序・JSON不正・名前欠落、詳細の通常形・空・件数欠落。project/local/未知scope、同じidの複数scope、重複、同名のkey順、description欠落後にinventory行が来る形は不足。詳細の最初の非空・非Source行をdescriptionとするため、descriptionのない出力では見出しを拾う可能性がある。出力実測はしていない。 |
| `codex/pluginsStatus.ts`、`codexPluginsStatus.test.ts`   | marketplace/installedの走査、必須id/name、installed限定、idによる先勝ち、説明とversionのfallback、originの各種別、詳細参照、警告、name/key順、providesの配列長。テストはremote/local、未install除外、無効状態、操作可否、marketplace参照、警告、root不正、必須値欠落、provides通常・不正。git/npm、欠落path、version差、空shortDescription、重複id、同名順は不足。                                                                                                 |
| `codex/appsStatus.ts`、`codexAppsStatus.test.ts`         | installed/readの型ガード、厳密なboolean判定、readの後勝ちMap、名前の詳細→runtime→id fallback、name/key順。テストは通常、id欠落、root不正、詳細結合とruntime fallback、整列。同一idの重複、id最終fallback、詳細のname欠落、同名key順、非booleanは不足。installed側は重複排除しない。                                                                                                                                                                                |

## 認証とProvider

| 実装とテスト                                           | 読んだ内容・不足                                                                                                                                                                                                                                           |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `claude/authStatus.ts`、`claudeAuthStatus.test.ts`     | JSON例外、recordの判定、loggedIn厳密判定、認証方式の対応・未知値維持、email/plan。通常2方式、未ログイン、欠落、JSON不正、null/array、未知方式を検査する。空方式、型違い、空文字のemail等は不足。                                                           |
| `codex/accountStatus.ts`、`codexAccountStatus.test.ts` | accountの有無、chatgpt/apiKey/amazonBedrock/未知type、email/plan。既知方式、null/欠落、email=null、未知方式、不正rootを検査する。空accountはログイン済みになり、空type・中間arrayは直接検査されていない。                                                  |
| `codex/provider.ts`、`claude/provider.ts`              | 固定capabilities、locator依存、一覧取得への委譲、tabTitleのnullish fallback。Claudeの`forkFromTurn: false`と「CLIにない」という説明は現在の分岐実装と不一致。ただし今回の全src参照検索でこのフラグを読む操作分岐はなく、UIを無効化する原因とは断定しない。 |
| `claude/authProbe.ts`                                  | execFileのtimeout、stderr優先の失敗理由、JSON変換、ログと失敗結果。専用テストはない。実行関数の同期throwやloggerのthrowを結果unionへ変換する処理はない。                                                                                                   |

## CLIプロセスと操作

| 実装とテスト                                               | 読んだ内容・不足                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `claude/usageProbe.ts`                                     | 実行中・最短間隔の早期return、開始時刻の更新、finallyでのrunning復旧、stdout収集、close/error/timeout、kill。専用テストはない。finishに一度限りのguardがなく、error/timeout後のcloseでも終了処理を再実行する。exit codeも評価しない。                                                                      |
| `claude/modelProbe.ts`、`claudeModelProbe.test.ts`         | spawn、initialize送信、改行buffer、モデル応答だけ採用、settled guard、timeout、error/close、stdin保護、終了処理。テストは`readModelsFromLine`だけで、通常JSON、別通知・壊れたJSON・array、エラー、モデルなしを検査する。read本体、受信分割、timeout、書込み失敗、終了順序は未検査。requestIdは照合しない。 |
| `claude/agentProbe.ts`、`claudeAgentProbe.test.ts`         | 上と同じプロセス経路でagentを抽出。テストは行parserの通常・別通知/不正・エラー・agentなしのみ。read本体の各非同期経路とrequestId照合が不足。                                                                                                                                                               |
| `claude/mcpProbe.ts`                                       | read/toggleの結果変換、名前検査、initializeと要求の連続送信、改行buffer、requestId照合、settled guard、timeout/error/close/stdin、kill。専用テストなし。initializeの完了を待たず次の要求を送る。safeWrite失敗後の即時失敗もない。                                                                          |
| `claude/skillsProbe.ts`                                    | reload送信、応答変換、警告、requestId照合、上記と同じプロセス終了経路。専用テストなし。                                                                                                                                                                                                                    |
| `claude/hooksProbe.ts`                                     | getSettings送信、応答なし/エラー/形違い/成功、trust等の非対応警告、requestId照合、同じプロセス終了経路。専用テストなし。                                                                                                                                                                                   |
| `claude/pluginsProbe.ts`                                   | list JSON、一覧失敗と形違い、先頭25件の詳細を順次取得、詳細失敗の継続、切捨て警告、execFileのtimeout。専用テストなし。詳細25件が各15秒かかる場合は一覧取得に加えて最大375秒かかり、途中の詳細失敗はsnapshotの警告に残らない。                                                                              |
| `claude/pluginsActions.ts`、`claudePluginsActions.test.ts` | enable/disable/uninstall/install、名前検査、scope有無、uninstallの-y、runnerへの委譲。fakeRunnerは正常結果を返し、各argvと入力拒否時の未実行を検査する。失敗結果・reject、installのscope、先頭ハイフンや制御文字は不足。                                                                                   |

一覧取得用のstream各所もBufferをチャンクごとに`toString()`している。UTF-8の途中で受信が分かれる問題は既存F01-03の影響範囲に含める。受信量上限はなく、終了まで改行のない入力を蓄積する。custom homeを子プロセスへ渡さない点は既存F01-01を参照し、重複指摘には数えない。

テストの`.every(...)`だけによる操作可否の検査は空配列でも成功する。同じfixtureの件数や内容を別テストでも検査している箇所はあるが、そのテスト単体で対象の存在を保証しているとは扱わない。
