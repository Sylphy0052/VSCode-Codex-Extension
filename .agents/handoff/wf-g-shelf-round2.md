# WF-G 棚更新 第2回 引き継ぎ

書き手は全体オーケストレーター。第1回の `wf-g-shelf-round1.md` は本ファイルで置き換える
（同一セッションの引き継ぎは1本、チェックポイントごとに上書き）。

## 今どこにいるか

第2回（#589 / #502 / #579 / #527）の実装は4本ともmainへマージ済み。本PRは棚の記帳のみで、
`src/` と `test/` には触っていない。第3回（#533 + #599）は担当が調査に着手済み。

- #589 → PR #641（`c9a376f6`）
- #502 → PR #642（`2a59aec4`）
- #579 → PR #644（`b2354234`）
- #527 → PR #645（`1a5a11da`）

## 測って確かめた事実と測ったコマンド

- 棚の項目数は変化なし。`grep -cE '^  - (\[#|T[0-9]|全体レビュー)' docs/roadmap/wf/wf-g.md` → `18`
  （既存行への追記のみ。新規行は足していない）。12行目の `**WF-G 横断の仕上げ**（18項目）` と一致
- `src`・`test` への変更はゼロ
- `.agents/handoff/wf-g-issue-579.md` を削除した。PR #644 が残したもので、
  PR #638 で決めた「Issueをクローズするコミットで同じく削除する」に反していた
- T26のスコープを実測し直した。`grep -rln "acquireVsCodeApi" src/view/*.ts` →
  `chatScript.ts` 2476 / `controlPanelScript.ts` 1141 / `workflowScript.ts` 1122 の3ファイル4739行
  （`conversationView.ts` 224行も返るがHTMLを組み立てる側なので除外）。
  当初の前提だった「`chatScript.ts` 1ファイル」は誤り
- ラベル表は16。3階層（tscが守る2件 / 開いた `Record<string, string>` 7件 /
  テンプレートリテラル内7件）に分かれ、直し方が違う。**この数字は #527 時点の観測で、
  #645 のマージで既に1件動いている**（`FAILURE_LABEL` が11→12）

## 未確認・推論

- **T26のラベル表の内訳は着手時に測り直すこと。**T26まで #533/#599・#485/#490・
  #524/#491・#541/#562・#551 が挟まる。数字を棚に残したのは変化の幅を伝えるためで、
  そのまま使うためではない
- 第3回の #533/#599 について、担当が「Issue本文のコード引用が実態と違う」を2件見つけている
  （`Codex:` 直書き vs `${LABEL}:` 変数、`deriveTitle` が2つ）。**未検証の第三の差分が
  あるかは測っていない**

## 次の一手

1. 本PRをレビューに出し、第2回の当事者（WF-G担当）に実態との突き合わせを依頼する
2. マージ後、第3回の案が上がってきたら判断する
3. Issue #636 は規約候補が16件まで来た。第8回（全体レビューの回）で整理する
