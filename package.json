{
  "name": "vscode-codex-extension",
  "displayName": "Agent Sessions",
  "description": "Codex / Claude Code のセッションをエディタタブとして開き、履歴から復元する",
  "version": "0.0.1",
  "license": "MIT",
  "publisher": "Sylphy0052",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/Sylphy0052/VSCode-Codex-Extension.git"
  },
  "engines": {
    "node": ">=20",
    "vscode": "^1.90.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "codexSessions",
          "title": "Agents",
          "icon": "resources/agents.svg"
        }
      ]
    },
    "views": {
      "codexSessions": [
        {
          "id": "codex.controlPanel",
          "type": "webview",
          "name": "設定"
        },
        {
          "id": "codex.sessions",
          "name": "履歴"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "codex.sessions",
        "contents": "このワークスペースのセッションはまだありません。\n[新しい会話（Codex）](command:codex.newChat)\n[新しい会話（Claude Code）](command:claude.newChat)\n全ワークスペースのセッションを見るには表示範囲を切り替えてください。"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "codex.newChat",
          "when": "view == codex.sessions",
          "group": "navigation@1"
        },
        {
          "command": "claude.newChat",
          "when": "view == codex.sessions",
          "group": "navigation@2"
        },
        {
          "command": "codex.showAllSessions",
          "when": "view == codex.sessions && codex.historyScope != all",
          "group": "navigation@3"
        },
        {
          "command": "codex.showWorkspaceSessions",
          "when": "view == codex.sessions && codex.historyScope == all",
          "group": "navigation@3"
        },
        {
          "command": "codex.refreshSessions",
          "when": "view == codex.sessions",
          "group": "navigation@4"
        },
        {
          "command": "agent.workflows.menu",
          "when": "view == codex.sessions",
          "group": "navigation@5"
        },
        {
          "command": "codex.filterSessions",
          "when": "view == codex.sessions",
          "group": "navigation@6"
        },
        {
          "command": "codex.clearSessionFilter",
          "when": "view == codex.sessions && codex.sessionFilterActive",
          "group": "navigation@7"
        },
        {
          "command": "agent.openPresetChat",
          "when": "view == codex.sessions && agent.hasSessionPresets",
          "group": "navigation@8"
        }
      ],
      "editor/title": [
        {
          "command": "codex.renameChat",
          "when": "activeWebviewPanelId == codex.chat",
          "group": "navigation@100"
        },
        {
          "command": "claude.renameChat",
          "when": "activeWebviewPanelId == claude.chat",
          "group": "navigation@100"
        },
        {
          "command": "codex.newChat",
          "when": "resourceScheme == file",
          "group": "navigation@100"
        },
        {
          "command": "claude.newChat",
          "when": "resourceScheme == file",
          "group": "1_new@1"
        },
        {
          "command": "codex.newChat",
          "when": "activeWebviewPanelId == codex.chat",
          "group": "navigation@101"
        },
        {
          "command": "codex.clearChat",
          "when": "activeWebviewPanelId == codex.chat",
          "group": "navigation@102"
        },
        {
          "command": "codex.handoffToNewSession",
          "when": "activeWebviewPanelId == codex.chat",
          "group": "navigation@103"
        },
        {
          "command": "claude.newChat",
          "when": "activeWebviewPanelId == claude.chat",
          "group": "navigation@101"
        },
        {
          "command": "claude.clearChat",
          "when": "activeWebviewPanelId == claude.chat",
          "group": "navigation@102"
        },
        {
          "command": "claude.handoffToNewSession",
          "when": "activeWebviewPanelId == claude.chat",
          "group": "navigation@103"
        }
      ],
      "editor/context": [
        {
          "command": "agent.sendSelectionToChat",
          "when": "editorHasSelection",
          "group": "9_agent@1"
        }
      ],
      "view/item/context": [
        {
          "command": "claude.openChat",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.claude/",
          "group": "inline@1"
        },
        {
          "command": "claude.openChat",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.claude/",
          "group": "1_open@0"
        },
        {
          "command": "codex.openChat",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "inline@1"
        },
        {
          "command": "codex.openChat",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "1_open@0"
        },
        {
          "command": "codex.openConversation",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "inline@2"
        },
        {
          "command": "codex.openConversation",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "1_open@0"
        },
        {
          "command": "codex.forkSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "inline@3"
        },
        {
          "command": "codex.forkSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "1_open@1"
        },
        {
          "command": "claude.forkSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.claude/",
          "group": "inline@2"
        },
        {
          "command": "claude.forkSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.claude/",
          "group": "1_open@1"
        },
        {
          "command": "codex.pinSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.(codex|claude)(\\.archived)?$/",
          "group": "2_manage@1"
        },
        {
          "command": "codex.unpinSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\..*\\.pinned$/",
          "group": "2_manage@1"
        },
        {
          "command": "codex.archiveSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex(\\.pinned)?$/",
          "group": "2_manage@2"
        },
        {
          "command": "codex.unarchiveSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex\\.archived(\\.pinned)?$/",
          "group": "2_manage@2"
        },
        {
          "command": "codex.deleteSession",
          "when": "view == codex.sessions && viewItem =~ /^codexSession\\.codex/",
          "group": "3_destructive@1"
        }
      ],
      "commandPalette": [
        {
          "command": "codex.openSession",
          "when": "false"
        },
        {
          "command": "codex.openChat",
          "when": "false"
        },
        {
          "command": "codex.openConversation",
          "when": "false"
        },
        {
          "command": "codex.forkSession",
          "when": "false"
        },
        {
          "command": "codex.archiveSession",
          "when": "false"
        },
        {
          "command": "codex.unarchiveSession",
          "when": "false"
        },
        {
          "command": "codex.deleteSession",
          "when": "false"
        },
        {
          "command": "codex.pinSession",
          "when": "false"
        },
        {
          "command": "codex.unpinSession",
          "when": "false"
        },
        {
          "command": "claude.openChat",
          "when": "false"
        },
        {
          "command": "claude.forkSession",
          "when": "false"
        },
        {
          "command": "agent.openPresetChat",
          "when": "agent.hasSessionPresets"
        }
      ]
    },
    "commands": [
      {
        "command": "codex.openSession",
        "title": "セッションを開く",
        "category": "Agent"
      },
      {
        "command": "codex.resumeSession",
        "title": "セッションを再開…",
        "category": "Agent"
      },
      {
        "command": "codex.resumeLast",
        "title": "直前のセッションを再開",
        "category": "Agent"
      },
      {
        "command": "codex.refreshSessions",
        "title": "更新",
        "category": "Agent",
        "icon": "$(refresh)"
      },
      {
        "command": "codex.showAllSessions",
        "title": "全ワークスペースを表示",
        "category": "Agent",
        "icon": "$(globe)"
      },
      {
        "command": "codex.showWorkspaceSessions",
        "title": "このワークスペースのみ表示",
        "category": "Agent",
        "icon": "$(filter-filled)"
      },
      {
        "command": "codex.filterSessions",
        "title": "履歴を絞り込む…",
        "category": "Agent",
        "icon": "$(search)"
      },
      {
        "command": "codex.clearSessionFilter",
        "title": "絞り込みを解除",
        "category": "Agent",
        "icon": "$(close)"
      },
      {
        "command": "codex.pinSession",
        "title": "ピン留めする",
        "category": "Agent",
        "icon": "$(pinned)"
      },
      {
        "command": "codex.unpinSession",
        "title": "ピン留めを外す",
        "category": "Agent",
        "icon": "$(pin)"
      },
      {
        "command": "codex.newChat",
        "title": "新しい会話（Codex）",
        "category": "Agent",
        "icon": "$(add)"
      },
      {
        "command": "codex.handoffToNewSession",
        "title": "新セッションへ引き継ぐ（Codex）",
        "category": "Agent",
        "icon": "$(arrow-right)"
      },
      {
        "command": "agent.openPresetChat",
        "title": "プリセットから新しい会話を開く…",
        "category": "Agent",
        "icon": "$(list-selection)"
      },
      {
        "command": "codex.clearChat",
        "title": "会話をクリアして新しく始める（Codex）",
        "category": "Agent",
        "icon": "$(clear-all)"
      },
      {
        "command": "claude.clearChat",
        "title": "会話をクリアして新しく始める（Claude Code）",
        "category": "Agent",
        "icon": "$(clear-all)"
      },
      {
        "command": "codex.renameChat",
        "title": "セッション名を変更",
        "category": "Agent",
        "icon": "$(edit)"
      },
      {
        "command": "codex.openChat",
        "title": "チャット画面で開く（Codex）",
        "category": "Agent",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "codex.openConversation",
        "title": "会話を開いて分岐する",
        "category": "Agent",
        "icon": "$(git-branch)"
      },
      {
        "command": "codex.forkSession",
        "title": "このセッションをforkする",
        "category": "Agent",
        "icon": "$(repo-forked)"
      },
      {
        "command": "codex.archiveSession",
        "title": "アーカイブする",
        "category": "Agent"
      },
      {
        "command": "codex.unarchiveSession",
        "title": "アーカイブを解除する",
        "category": "Agent"
      },
      {
        "command": "codex.deleteSession",
        "title": "削除する",
        "category": "Agent"
      },
      {
        "command": "codex.showUsage",
        "title": "使用量を表示",
        "category": "Agent"
      },
      {
        "command": "codex.showLog",
        "title": "ログを表示",
        "category": "Agent"
      },
      {
        "command": "claude.newChat",
        "title": "新しい会話（Claude Code）",
        "category": "Agent",
        "icon": "$(sparkle)"
      },
      {
        "command": "claude.handoffToNewSession",
        "title": "新セッションへ引き継ぐ（Claude Code）",
        "category": "Agent",
        "icon": "$(arrow-right)"
      },
      {
        "command": "claude.openChat",
        "title": "チャット画面で開く（Claude Code）",
        "category": "Agent",
        "icon": "$(sparkle)"
      },
      {
        "command": "claude.forkSession",
        "title": "このセッションをforkする（Claude Code）",
        "category": "Agent",
        "icon": "$(repo-forked)"
      },
      {
        "command": "claude.reloadSkills",
        "title": "skillsを読み直す（Claude Code）",
        "category": "Agent"
      },
      {
        "command": "claude.renameChat",
        "title": "セッション名を変更（Claude Code）",
        "category": "Agent",
        "icon": "$(edit)"
      },
      {
        "command": "agent.workflows.menu",
        "title": "ワークフロー…",
        "category": "Agent",
        "icon": "$(type-hierarchy)"
      },
      {
        "command": "agent.workflows.run",
        "title": "ワークフローを実行…",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.runProgram",
        "title": "プログラムを実行…",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.stop",
        "title": "ワークフローを停止…",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.stopProgram",
        "title": "プログラムを停止…",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.roadmap",
        "title": "ロードマップを生成…",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.view",
        "title": "ワークフローViewを開く",
        "category": "Agent"
      },
      {
        "command": "agent.workflows.plan",
        "title": "ゴール文からワークフローを生成…",
        "category": "Agent"
      },
      {
        "command": "agent.sendSelectionToChat",
        "title": "Agentへ送る",
        "category": "Agent"
      }
    ],
    "configuration": {
      "title": "Agent Sessions",
      "properties": {
        "codex.executablePath": {
          "type": "string",
          "default": "codex",
          "scope": "machine",
          "markdownDescription": "codex実行ファイルのパス。パス区切りを含まない場合はPATHから探す。\n\nリポジトリ側の設定から任意のバイナリを起動されないよう machine スコープに固定している。"
        },
        "codex.codexHome": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "CODEX_HOME のパス。空なら環境変数、それも無ければ `~/.codex` を使う。"
        },
        "codex.additionalArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "machine",
          "markdownDescription": "codex起動時に追加で渡す引数。"
        },
        "codex.sandbox": {
          "type": "string",
          "enum": [
            "",
            "read-only",
            "workspace-write",
            "danger-full-access"
          ],
          "default": "",
          "scope": "machine",
          "markdownDescription": "サンドボックスの方針。空なら `~/.codex/config.toml` に委譲する。会話の途中で変えると次の発言から効く。\n\n画面の「承認」（3段階）を選ぶと `codex.approvalMode` / `codex.approvalsReviewer` と一緒に書き換わる。"
        },
        "codex.sandboxWritableRoots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "machine",
          "markdownDescription": "`workspace-write` のときに作業フォルダの外で書き込みを許す場所。**絶対パスのみ**（相対パスは無視する）。TUIの `/sandbox-add-read-dir` に相当する。"
        },
        "codex.sandboxNetworkAccess": {
          "type": "boolean",
          "default": false,
          "scope": "machine",
          "markdownDescription": "`workspace-write` のときにネットワークへ出られるようにする。"
        },
        "codex.approvalMode": {
          "type": "string",
          "enum": [
            "",
            "untrusted",
            "on-request",
            "never"
          ],
          "default": "",
          "scope": "machine",
          "markdownDescription": "コマンド実行の承認方針。空なら `~/.codex/config.toml` に委譲する。\n\n設定パネルやチャット画面の「承認」（全確認 / Auto / 全承認の3段階）を選ぶと、この項目と `codex.sandbox` / `codex.approvalsReviewer` がまとめて書き換わる。個別に指定した場合、3段階のどれとも一致しない組み合わせは画面に「カスタム」と表示される（対応表は docs/approval-modes.md）。"
        },
        "codex.approvalsReviewer": {
          "type": "string",
          "enum": [
            "",
            "user",
            "auto_review"
          ],
          "enumDescriptions": [
            "Codex側の既定に委譲する",
            "承認要求を画面のカードで人へ回す",
            "承認要求をCodex内部のsubagentが自動で判定する"
          ],
          "default": "",
          "scope": "machine",
          "markdownDescription": "承認要求を誰へ回すか。承認方針（いつ承認を求めるか）とは別の軸。`auto_review` は人の代わりにCodex内部のsubagentがリスクを判定して承認・拒否するため、選ぶ前に確認ダイアログを出す。\n\n画面の「承認」で `Auto`（承認をエージェントに任せる）を選ぶと `auto_review` になる。Claude Codeの `auto` と判定主体を揃えるため。"
        },
        "codex.bypassApprovalsAndSandbox": {
          "type": "boolean",
          "default": false,
          "scope": "machine",
          "markdownDescription": "**危険**。承認もサンドボックスも一切かけずに実行する（`--dangerously-bypass-approvals-and-sandbox`）。外側で隔離済みの環境（コンテナ・使い捨てVM）向け。有効にすると `codex.sandbox` / `codex.approvalMode` / `codex.approvalsReviewer` は渡さない。会話を開くたびに確認ダイアログを出す。"
        },
        "codex.model": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "使用するモデル。空なら `-m` を渡さない。"
        },
        "codex.reasoningEffort": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "推論の深さ (`model_reasoning_effort`)。空なら `~/.codex/config.toml` に委譲する。選択肢はモデルごとに異なるため、サイドバーの設定パネルから選ぶことを推奨。"
        },
        "codex.profile": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "`-p` に渡すCodexのプロファイル名。"
        },
        "codex.history.scope": {
          "type": "string",
          "enum": [
            "workspace",
            "all"
          ],
          "default": "workspace",
          "scope": "window",
          "markdownDescription": "履歴一覧に出す範囲。`workspace` は現在のワークスペース配下のセッションのみ。"
        },
        "codex.history.maxEntries": {
          "type": "number",
          "default": 200,
          "minimum": 1,
          "scope": "window",
          "markdownDescription": "履歴一覧を構築する際に読む最大件数。更新時刻の新しい順に絞る。"
        },
        "codex.history.groupBy": {
          "type": "string",
          "enum": [
            "date",
            "folder",
            "none"
          ],
          "enumDescriptions": [
            "今日・昨日・今週・それ以前でグループ化する",
            "作業ディレクトリ別にグループ化する（全ワークスペース表示のときに使いやすい）",
            "グループ化しないフラットな一覧（従来の表示）"
          ],
          "default": "date",
          "scope": "window",
          "markdownDescription": "履歴一覧のグループ化。ピン留めしたセッションはどの設定でも先頭の「ピン留め」グループへ出る（`none`を除く）。"
        },
        "agent.activityLog.enabled": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "markdownDescription": "この拡張機能から実行したセッションを日報/週報用のバッファへ記録する。記録するのはセッションごとに1行（時刻・作業ディレクトリ・要約）だけで、会話本文は書き出さない。"
        },
        "agent.activityLog.dir": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "作業記録の出力先ディレクトリ。空なら環境変数 `DAILY_BUFFER_DIR`、それも無ければ `~/workspace/dairy/.buffer` を使う。"
        },
        "agent.chat.renderMarkdown": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "markdownDescription": "チャット画面の応答本文を見出し・箇条書き・強調・インラインコード・コードブロック・リンクとして描画する。コードブロックには「コピー」「エディタへ挿入」「新規ファイルで開く」を付ける。無効にすると従来どおり生テキストのまま表示する（Codex/Claude Code両画面共通）。"
        },
        "agent.chat.sendOn": {
          "type": "string",
          "enum": [
            "ctrlEnter",
            "enter"
          ],
          "enumDescriptions": [
            "Ctrl+Enter / Cmd+Enterで送信する。Enterは改行のまま（従来の挙動）",
            "Enterで送信し、Shift+Enterで改行する。Ctrl+Enter / Cmd+Enterでも送信できる"
          ],
          "default": "ctrlEnter",
          "scope": "window",
          "markdownDescription": "チャット画面の入力欄でEnterを送信に使うか。候補メニュー（`/` `@`）を開いている間はEnterが候補の確定として優先され、IME変換中のEnterは送信しない。**Codex画面のみ**（Claude Code画面は今回のスコープ外で常に`ctrlEnter`のまま）。"
        },
        "agent.chat.composerButtons": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "attach",
              "loopToggle",
              "compact",
              "claudeImport",
              "recap",
              "planToggle",
              "fastToggle",
              "review",
              "exportTranscript",
              "workflowMenu"
            ],
            "enumDescriptions": [
              "画像を添付する",
              "ループ（同じ指示を条件成立まで繰り返す）を開く",
              "会話を要約に置き換えてコンテキストを空ける（圧縮）",
              "他エージェントから設定をインポートする",
              "会話の1行要約をいま作る",
              "計画モード（読み取りだけに絞る）を切り替える",
              "高速モード（Fast mode）を切り替える",
              "コードレビューを実行する",
              "会話全体をMarkdownとして取り出す（エクスポート）",
              "ワークフローの実行・View・生成・停止を選ぶ"
            ]
          },
          "default": [
            "attach",
            "loopToggle",
            "compact",
            "claudeImport"
          ],
          "scope": "window",
          "markdownDescription": "チャット入力欄のアイコン列（`#composerIconRow`）の表に直接並べるボタン。ここに無いボタンは「…」メニューへ畳まれる（無くなるわけではない、issue #296）。既定は変更前の並びの先頭4つ。未知のIDや重複を含む場合は既定へ丸める。Codex/Claude Code両画面共通（実行時の条件で元から出ないボタン、例えば`fastToggle`はFast mode非対応環境では終始非表示、を指定しても、その状態のままなら出ない）。"
        },
        "agent.notifications.approvalPending": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "markdownDescription": "承認要求が出た直後、そのタブが見えていなければ通知を出す（Codex/Claude Code両画面共通）。同じ要求で通知が重複することはない。無効にすると承認待ちの通知を一切出さない（タブ名の印・履歴ツリーの表示は変わらない）。"
        },
        "agent.notifications.turnComplete": {
          "type": "boolean",
          "default": false,
          "scope": "window",
          "markdownDescription": "ターンが完了した直後、そのタブが見えていなければ通知を出す（Codex/Claude Code両画面共通）。既定は無効（頻度が高く、承認待ちほど対応が急がないため）。"
        },
        "agent.sessionPresets": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "name",
              "provider"
            ],
            "properties": {
              "name": {
                "type": "string",
                "description": "プリセットの表示名（QuickPickのラベル）。空・重複は無視される。"
              },
              "provider": {
                "type": "string",
                "enum": [
                  "codex",
                  "claude"
                ],
                "description": "開く先。codexまたはclaude。"
              },
              "model": {
                "type": "string",
                "description": "使用するモデル。空なら拡張機能の現在の設定（`codex.model` / `claude.model`）を渡さず、CLI側の既定に委譲する。"
              },
              "effort": {
                "type": "string",
                "description": "推論の深さ。空ならCLI側の既定に委譲する。"
              },
              "approvalMode": {
                "type": "string",
                "enum": [
                  "",
                  "untrusted",
                  "on-request",
                  "never",
                  "manual",
                  "auto",
                  "acceptEdits",
                  "plan",
                  "dontAsk",
                  "bypassPermissions"
                ],
                "description": "承認方針（Codexは`untrusted`/`on-request`/`never`、Claudeは`permissionMode`の値である`manual`/`auto`/`acceptEdits`/`plan`/`dontAsk`/`bypassPermissions`）。providerに応じてどちらかの値域を使う。拡張機能側の現在の設定より緩い方向へは適用されず、無視した場合は警告を出す。"
              },
              "sandbox": {
                "type": "string",
                "enum": [
                  "",
                  "read-only",
                  "workspace-write",
                  "danger-full-access"
                ],
                "description": "サンドボックスの方針（`read-only`/`workspace-write`/`danger-full-access`）。Codexのみ意味を持つ。拡張機能側の現在の設定より緩い方向へは適用されない。"
              },
              "workingDirectory": {
                "type": "string",
                "description": "開始時の作業ディレクトリ。ワークスペースフォルダ配下の絶対パスに限る。外を指す場合は無視して警告を出す。"
              }
            }
          },
          "default": [],
          "scope": "resource",
          "markdownDescription": "名前付きのセッションプリセット。コマンド`エージェント: プリセットから新しい会話を開く…`（コマンドパレット・履歴ビューのタイトルバー）から選んで、モデル・承認・サンドボックス・作業ディレクトリをまとめて指定した新しい会話を開ける。\n\n`approvalMode`（Codexの承認方針・Claudeの`permissionMode`）と`sandbox`（Codexのみ）は、拡張機能側の現在の設定（`codex.approvalMode` / `codex.sandbox` / `claude.permissionMode`）より緩い方向へは適用されない。緩める指定は無視し、選択時に警告を出す（ワークフロー定義YAMLのクランプと同じ方針。設計書 §14.56・§16.16）。`model` / `effort`はクランプ対象外で自由に指定できる。\n\n`workingDirectory`はワークスペースフォルダ配下の絶対パスに限る。外を指す値は無視して警告を出し、通常の作業ディレクトリの決め方（マルチルートならQuickPickで選択、単一フォルダならそのフォルダ）へフォールバックする。\n\nプリセット自体は拡張機能の設定より権限を緩められないため、`machine`スコープには固定していない。リポジトリの`.vscode/settings.json`からプリセットを追加できる。プリセットが1件も無いときはコマンドを出さない。"
        },
        "agent.workflows.dir": {
          "type": "string",
          "default": ".agents/workflows",
          "scope": "resource",
          "markdownDescription": "ワークフロー定義ファイル（`.yaml` / `.yml`）を探すディレクトリ。ワークスペースフォルダからの相対パス。"
        },
        "agent.workflows.allowAutoApprove": {
          "type": "boolean",
          "default": false,
          "scope": "machine",
          "markdownDescription": "ワークフロー定義（YAML）の `autoApprove: true` を有効化できるようにする。無効のままだと、YAMLに何と書かれていても全ての承認要求を人へ回す。リポジトリ側のYAMLから無人実行を勝手に有効化されないよう machine スコープに固定している（設計書 §16.16）。"
        },
        "agent.workflows.allowClaudeBypassPermissions": {
          "type": "boolean",
          "default": false,
          "scope": "machine",
          "markdownDescription": "**危険**: `claude.permissionMode` が `bypassPermissions` のとき、ワークフローのClaudeタスクもその設定のまま実行する。有効にすると Claude Code CLI がツール実行の可否を一切問い合わせなくなるため、この拡張機能の危険判定（`rm -rf` 相当・force push・外部送信・作業ディレクトリ外への操作など）が**全て無効**になり、ワークフローが生成したコマンドは内容にかかわらず実行される。無効（既定）のときは、そのタスクだけ `acceptEdits` へ読み替えて危険判定を働かせる（設計書 §16.16）。リポジトリ側の設定から勝手に有効化されないよう machine スコープに固定している。"
        },
        "agent.workflows.replyTimeoutSec": {
          "type": "number",
          "default": 300,
          "minimum": 1,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "タスク間のメッセージング（`send_message` の `expectReply: true`）で、返信を待つ時間の上限（秒）。超えたタスクは返信を待たずに再開する（設計書 §16.21）。上限は`2147483`秒（約24.8日、`setTimeout`の32bit上限）。範囲外（1未満・上限超過・非数値）の値は既定値（300秒）にフォールバックする。"
        },
        "agent.workflows.mergeApprovalTimeoutSec": {
          "type": "number",
          "default": 3600,
          "minimum": 1,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "衝突解決セッション（設計書 §16.17「コンフリクト」）が承認待ちのまま止まってよい時間の上限（秒）。超えたら自動的にセッションを停止し、対象タスクを`blocked`にする（`git merge --abort`は呼ばない。統合worktreeは衝突した状態のまま残り、Viewの「再マージ」で再開できる）。承認待ちで**ない**（LLMが作業中の）時間は計測に含まれない。権限には関わらないため`forge`/`finalMerge`ほど強い制限は要らない。上限は`2147483`秒（約24.8日、`setTimeout`の32bit上限）。範囲外（1未満・上限超過・非数値）の値は既定値（3600秒）にフォールバックする。"
        },
        "agent.workflows.taskApprovalTimeoutSec": {
          "type": "number",
          "default": 3600,
          "minimum": 1,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "通常タスクの承認待ち（`waitingApproval`）が止まってよい時間の上限（秒）。超えたら自動的にタスクを停止し`failed`（理由: `taskApprovalTimedOut`）にする。**`agent.workflows.mergeApprovalTimeoutSec`とは別のキー**（あちらは衝突解決セッション専用で`blocked`にする。設計書 §16.39）。危険と判定された操作を時間切れで暗黙に承認したことにはしない。`retries`の自動再試行の対象にもしない。権限には関わらないため`forge`/`finalMerge`ほど強い制限は要らない。上限は`2147483`秒（約24.8日、`setTimeout`の32bit上限）。範囲外（1未満・上限超過・非数値）の値は既定値（3600秒）にフォールバックする。"
        },
        "agent.workflows.finalMergeDecisionTimeoutSec": {
          "type": "number",
          "default": 900,
          "minimum": 1,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "`agent.workflows.finalMerge` が `orchestrator` のとき、統合PR/MRの作成後にオーケストレーターが `decide_final_merge` で応答するのを待つ時間の上限（秒）。超えたら応答が無かったものとして自動的に `hold`（マージせずPR/MRを残す）へ倒す（判断を待って無限に止まらないため。設計書 §16.26）。`confirm`（人の承認待ち）には効かない。権限には関わらないため`forge`/`finalMerge`ほど強い制限は要らない。上限は`2147483`秒（約24.8日、`setTimeout`の32bit上限）。範囲外（1未満・上限超過・非数値）の値は既定値（900秒）にフォールバックする。"
        },
        "agent.workflows.ciWaitTimeoutSec": {
          "type": "number",
          "default": 1800,
          "minimum": 1,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "統合PR/MRをマージする前に、CIチェック（`statusCheckRollup` / パイプライン）の完了を待つ時間の上限（秒）。超えたら赤（CI失敗）と同じ扱いでタスクを失敗として確定する（設計書 §16.36）。CIが1件も設定されていないリポジトリでは待たずに即マージする。権限には関わらないため`forge`/`finalMerge`ほど強い制限は要らない。上限は`2147483`秒（約24.8日、`setTimeout`の32bit上限）。範囲外（1未満・上限超過・非数値）の値は既定値（1800秒）にフォールバックする。"
        },
        "agent.workflows.ciUpdateBranchMaxRetries": {
          "type": "number",
          "default": 2,
          "minimum": 0,
          "maximum": 100,
          "scope": "machine-overridable",
          "markdownDescription": "統合PR/MRのマージが「baseの最新でない」ことで拒否されたときに、`gh pr update-branch` / `glab mr rebase` で取り込み直してから再試行する最大回数（初回のマージ試行は含まない）。取り込み直すたびにCIの完了を待ち直す。上限を超えたら失敗として確定する（設計書 §16.36）。0にすると取り込み直しをせず初回の失敗をそのまま確定する。範囲外（0未満・100超過・非整数）の値は既定値（2回）にフォールバックする。"
        },
        "agent.workflows.reviewCommentPollIntervalSec": {
          "type": "number",
          "default": 600,
          "minimum": 0,
          "maximum": 2147483,
          "scope": "machine-overridable",
          "markdownDescription": "統合PR/MRのレビューコメントを取得する間隔（秒）。統合PR/MRを作成できた実行だけが対象で、新しく見つかったコメントはオーケストレーターへ通知され（設計書 §16.30・§16.23）、対応するタスクの調整（`add_task`等）に人の承認は挟まない。適用した内容はワークフローViewの警告欄へ全文で残る。0にすると取得しない（既定は控えめに置き、APIを叩き続けない設計判断）。範囲外（0未満・上限超過・非整数）の値は既定値（600秒）にフォールバックする。"
        },
        "agent.workflows.roadmapDir": {
          "type": "string",
          "default": "docs/roadmap",
          "scope": "machine-overridable",
          "markdownDescription": "ロードマップ（`workflow.roadmap` で生成するMarkdown）の出力先ディレクトリ。ワークスペースフォルダからの相対パス。出力先はワークスペースフォルダの配下に限る（設計書 §16.16・§16.19）。"
        },
        "agent.workflows.pseudoWorktreeExclude": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "node_modules",
            ".venv",
            "dist",
            "out"
          ],
          "scope": "machine-overridable",
          "markdownDescription": "ワークスペースがgitの作業ツリーでない場合の隔離（疑似worktree）で、複製から除外するディレクトリ名。深さを問わず名前が一致するディレクトリを除外する。`.agents/worktrees` 自身は常に除外される（設計書 §16.20）。指定できるのはディレクトリ名だけで、`.git`（`.GIT` 等の亜種を含む）・パス区切りを含む値・絶対パス・`..`・`.` は受け付けない（前後の空白は取り除いてから判定する）。1つでも含まれていれば設定全体を既定値へ戻し、警告を通知する。"
        },
        "agent.workflows.forge": {
          "type": "string",
          "enum": [
            "auto",
            "github",
            "gitlab",
            "none"
          ],
          "default": "auto",
          "scope": "machine",
          "markdownDescription": "PR/MRの作成に使うホストの選択。`auto` は `origin` のリモートURLから判定する（`github.com` はGitHub、ホスト名に `gitlab` を含めばGitLab）。自己ホストのGitHub Enterpriseなど名前から判定できない場合は `github` / `gitlab` を明示する。`none` はPR/MRを作らない。実行するコマンド（`gh` / `glab`）の選択にあたるため、machineスコープに固定している。リポジトリの `.vscode/settings.json` からは変えられない（設計書 §16.16・§16.18）。"
        },
        "agent.workflows.pullRequest": {
          "type": "string",
          "enum": [
            "none",
            "integration",
            "per-task"
          ],
          "default": "per-task",
          "scope": "machine-overridable",
          "markdownDescription": "作るPR/MRの層。`per-task` はタスクブランチ→統合ブランチ（タスクごと）と統合ブランチ→main（runごと1本）の両方、`integration` は統合ブランチ→mainのみ、`none` はPR/MRを作らない（設計書 §16.18）。"
        },
        "agent.workflows.finalMerge": {
          "type": "string",
          "enum": [
            "auto",
            "orchestrator",
            "confirm",
            "pr-only"
          ],
          "default": "orchestrator",
          "scope": "machine",
          "markdownDescription": "統合ブランチ→mainのPR/MRを作った後、マージするかどうかをどう決めるか。`auto` はPR/MRを作ってそのままマージする（従来の既定）。`orchestrator` はPR/MRを作り、オーケストレーターの判断でマージする（**新しい既定**。判断とその理由は必ずワークフローViewの警告欄に残り、`agent.workflows.finalMergeDecisionTimeoutSec` 秒応答が無ければ自動的に `hold` として扱う）。`confirm` はPR/MRを作って人の承認を待ち、承認されたときだけマージする。`pr-only` はPR/MRを作った時点でrunを終え、mainへの書き込みは人が行う。いずれもマージ（`gh pr merge --merge` / `glab mr merge --remove-source-branch`）を実行するかどうかを決めるため、machineスコープに固定している。リポジトリの `.vscode/settings.json` から緩めることはできない。MRの自己マージを禁じる運用規約を持つ組織では `pr-only` にする（設計書 §16.16・§16.18・§16.26）。"
        },
        "agent.workflows.branchNaming": {
          "type": "string",
          "enum": [
            "wf",
            "conventional"
          ],
          "default": "wf",
          "scope": "machine-overridable",
          "markdownDescription": "タスクブランチの命名方式。`wf` は `wf/<runId>/<taskId>`。`conventional` は `<type>/<IID>/<slug>`（例 `feature/123/t1-a1b2c3d4`）で、この形のブランチ名を要求する運用規約に合わせる。`conventional` はタスクに `issue` が指定されている場合にだけ効き、指定が無いタスクは `wf` 形式へ落ちる（設計書 §16.6）。"
        },
        "agent.workflows.draftPullRequest": {
          "type": "boolean",
          "default": false,
          "scope": "machine-overridable",
          "markdownDescription": "PR/MRをDraftとして作り、統合ブランチへのマージが済んでからreadyへ切り替えるか。`gh pr create --draft` / `glab api --field=draft=true` で作り、`gh pr ready` / `glab mr update --ready` で外す（設計書 §16.18）。"
        },
        "agent.workflows.createTaskIssue": {
          "type": "boolean",
          "default": false,
          "scope": "machine-overridable",
          "markdownDescription": "タスクの開始時にIssueを起票し、PR/MRの本文から参照するか。`pullRequest: per-task` のときだけ有効（`gh issue create` / `glab api projects/:id/issues`）。タスクのYAML側で既に `issue` が指定されている場合は起票しない（設計書 §16.31）。"
        },
        "agent.workflows.reviewTaskPullRequest": {
          "type": "boolean",
          "default": false,
          "scope": "machine-overridable",
          "markdownDescription": "PR/MRを作った後、ローカルマージの前に、拡張が別の読み取り専用セッションでレビューさせるか。forgeの「人のレビューを待つ」機構ではなく、分解レビュー（`reviewWorkflowPlan`）と同じ方式で、応答を待ち続けることはない。結果は警告として記録するだけでマージは止めない（設計書 §16.31）。"
        },
        "agent.workflows.stallRepeatCount": {
          "type": "number",
          "default": 4,
          "minimum": 2,
          "maximum": 50,
          "scope": "machine-overridable",
          "markdownDescription": "タスクのループが停滞したとみなすまでに必要な、同一の応答が連続する回数。直近の応答要約がこの回数だけ連続して同一になったら、送信回数の上限（`maxIterations`）を使い切る前にループを止め、失敗とは区別できる状態（停滞）でオーケストレーターへ通知する（設計書 §16.27）。既定値は誤検知しない側（大きめ）に振ってある。範囲外（2未満・50超過・非整数）の値は既定値（4）にフォールバックする。"
        },
        "agent.workflows.maxAskUserPerRun": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 20,
          "scope": "machine-overridable",
          "markdownDescription": "オーケストレーターが `ask_user`（人へ確認する道具。設計書 §16.33）を1つの実行で呼べる回数の上限。担当領域をまたぐ変更・設計の前提を変える変更・受入基準を下げる判断・同じ失敗を3回繰り返して打つ手が尽きた場合に限って使う道具で、乱発を防ぐため設定ではなく既定の挙動として回数に上限を置く。上限に達した以降の呼び出しは拒否され、オーケストレーターは自分で判断するか `decide_final_merge` の `hold` で止めるよう促される。範囲外（1未満・20超過・非整数）の値は既定値（3回）にフォールバックする。"
        },
        "agent.workflows.autoResume": {
          "type": "boolean",
          "default": true,
          "scope": "machine-overridable",
          "markdownDescription": "ウィンドウのリロードやWSLの停止・再起動でワークフロー実行が中断された後、条件を満たせば自動的に再開するか（設計書 §16.35）。人が「全体停止」で止めた実行、他の理由で失敗しているタスクが混ざっている実行、危険な操作の確認（`allow`）が必要なタスクが混ざっている実行は対象外で、これまでどおりワークフロービューから手動で再実行する。`false` にすると自動再開そのものを行わず、従来どおり常に手動で再実行する。"
        },
        "agent.workflows.maxAutoResumeAttempts": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 20,
          "scope": "machine-overridable",
          "markdownDescription": "自動再開（`agent.workflows.autoResume`、設計書 §16.35）を試みる回数の上限。同じ実行がクラッシュと自動再開を繰り返し続けて壊れるのを防ぐための値で、上限に達した以降はワークフロービューにその旨を表示したまま自動再開をあきらめ、手動での再実行を待つ。範囲外（1未満・20超過・非整数）の値は既定値（3回）にフォールバックする。"
        },
        "claude.executablePath": {
          "type": "string",
          "default": "claude",
          "scope": "machine",
          "markdownDescription": "claude実行ファイルのパス。パス区切りを含まない場合はPATHから探す。\n\nリポジトリ側の設定から任意のバイナリを起動されないよう machine スコープに固定している。"
        },
        "claude.configDir": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "markdownDescription": "Claude Codeの設定ディレクトリ。空なら環境変数 `CLAUDE_CONFIG_DIR`、それも無ければ `~/.claude` を使う。"
        },
        "claude.additionalArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "machine",
          "markdownDescription": "claude起動時に追加で渡す引数。"
        },
        "claude.permissionMode": {
          "type": "string",
          "enum": [
            "",
            "manual",
            "auto",
            "acceptEdits",
            "plan",
            "dontAsk",
            "bypassPermissions"
          ],
          "default": "",
          "scope": "machine",
          "markdownDescription": "ツール実行の承認方針。空なら Claude Code 側の設定に委譲する。`bypassPermissions` は全確認を飛ばすため、起動前に確認ダイアログを出す。\n\n画面の「承認」（全確認 / Auto / 全承認の3段階）はこの項目へ `manual` / `auto` / `bypassPermissions` として展開される。`acceptEdits` / `plan` / `dontAsk` は3段階に対応しないため、選ぶと画面には「カスタム」と表示される（対応表は docs/approval-modes.md）。"
        },
        "claude.model": {
          "type": "string",
          "default": "opus",
          "scope": "machine-overridable",
          "markdownDescription": "使用するモデル。エイリアス (`opus` / `sonnet` / `fable` / `haiku`) か正式名を指定する。空なら `--model` を渡さず、Claude Code 側の設定 (`~/.claude/settings.json`) に委譲する。"
        },
        "claude.effort": {
          "type": "string",
          "enum": [
            "",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "medium",
          "scope": "machine-overridable",
          "markdownDescription": "推論の深さ (`--effort`)。空なら渡さず、Claude Code 側の設定に委譲する。"
        },
        "claude.agent": {
          "type": "string",
          "default": "",
          "scope": "machine-overridable",
          "markdownDescription": "使用するエージェント (`--agent`)。組込エージェント (`claude` / `Explore` / `Plan` / `general-purpose` など) やユーザー定義のカスタムエージェントの名前を指定する。空なら渡さず、Claude Code 側の `agent` 設定に委譲する。起動時にのみ効き、会話の途中では切り替えられない。"
        }
      }
    },
    "keybindings": [
      {
        "command": "codex.newChat",
        "key": "ctrl+k x",
        "mac": "cmd+k x",
        "when": "!terminalFocus && !inputFocus"
      },
      {
        "command": "claude.newChat",
        "key": "ctrl+k l",
        "mac": "cmd+k l",
        "when": "!terminalFocus && !inputFocus"
      },
      {
        "command": "codex.resumeLast",
        "key": "ctrl+k b",
        "mac": "cmd+k b",
        "when": "!terminalFocus && !inputFocus"
      },
      {
        "command": "agent.workflows.view",
        "key": "ctrl+k a",
        "mac": "cmd+k a",
        "when": "!terminalFocus && !inputFocus"
      }
    ]
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --target=node20",
    "watch": "npm run build -- --sourcemap --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "compile:integration": "tsc -p tsconfig.integration.json",
    "build:integration": "npm run build && npm run compile:integration",
    "test:integration": "npm run build:integration && vscode-test",
    "test:integration:xvfb": "npm run build:integration && bash scripts/xvfb-vscode-test.sh",
    "test:external-cli": "node scripts/run-external-cli-tests.mjs",
    "check": "bash scripts/check.sh",
    "package": "npm run build && vsce package --no-dependencies"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.90.0",
    "@vitest/coverage-v8": "^4.1.11",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "eslint": "^9.17.0",
    "mocha": "^11.8.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
