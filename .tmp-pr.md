## 概要

ステータスバーのCodex使用量に5目盛りのゲージを添える。`$(pulse) Codex ▮▮▮▯▯ 62%` の形。数字だけだと残りの少なさに気付くのに読解が要るため、形でも分かるようにする。

Closes #756（epic #762）

## 着手前の実測

`src/view/usageStatusBar.ts:68-70`（変更前）は `$(pulse) Codex ${percent}%` に加えてリセット時刻を並べるだけで、ゲージに相当する要素は無かった。issue本文の記載と一致していた（他のepic子issueと違い、先行実装は無し）。

## 変更内容

- `src/codex/usage.ts`: `formatUsageGauge(usedPercent, cells = USAGE_GAUGE_CELLS)` を追加。表示の組み立ては純関数に閉じ、`usageStatusBar.ts` は文字列を差し込むだけにした
- `src/view/usageStatusBar.ts`: `update` のみ変更。`isGaugeEnabled()` が `true` のときゲージを数字の手前へ入れる
- `package.json`: `codex.usage.statusBarGauge`（boolean、既定 `true`、scope `window`）を追加
- `docs/design.md`（「使用量の表示」）、`docs/manual-test.md`（C-56）を追記

## 設計上の判断

- **目盛り数を固定した**。埋まった分だけ文字を描く方式だと使用率で文字数が変わり、ステータスバーの右隣の項目が左右に揺れる。`▮`（U+25AE）と `▯`（U+25AF）はどちらもGeometric Shapesの同系字形で、幅が揃う前提で選んだ（実機のフォント差はC-56で見る）
- **端は丸め切らない**。0%でないのに全部空、100%でないのに全部埋まる、という誤読を避けるため、0%と100%以外は必ず1目盛り以上を埋め、1目盛り以上を空けて残す
- **設定で戻せるようにした**。issue本文が「設定で切り替えられるようにするか、ゲージのみ/数字のみを選べるようにするかを実装時に判断する」としていた点。ステータスバーは他の拡張機能と場所を取り合うため、5文字分の増加を嫌う人向けの逃げ道としてboolean 1つに留めた（3択のenumにするほどの要求は読み取れなかった）
- 背景色の閾値（75%/90%）と未取得時の `Codex --` は変えていない。Claude Code側（`claudeItem`）は消費率を返さないため対象外

## テスト

- `test/unit/usage.test.ts`: `formatUsageGauge` の目盛り、幅の不変、端の丸め、範囲外、未取得、目盛り数の可変
- `test/unit/usageStatusBar.test.ts`（新規）: 既定でゲージが付くこと、設定 `false` で従来表示へ戻ること、使用量を変えても幅が変わらないこと（ゲージ部分を取り出せているかの陽性対照つき）、背景色が従来どおりであること、未取得時の表示が変わらないこと

```
npm run check
=== ALL PASS ===
Test Files  196 passed (196)
Tests  4306 passed (4306)
```

## 未実施

**実機のVS Codeでの目視確認はしていない。** 以下はコードからの推論に留まる。`docs/manual-test.md` の C-56 として手順を残した。

- ステータスバーの実フォントで `▮` と `▯` の幅が実際に揃うか
- ゲージを足したことで右隣の項目が押し出されないか
