# ビルド成果物と外部から取得したもの。eslint.config.mjs の ignores と揃える
dist/
out/
coverage/
node_modules/
.vscode-test/
.claude/

# 評価ハーネスの凍結ファイル。バイト列の sha256 で実行時の記録と照合するため、整形で
# 1バイトでも変わると集計が止まる。経緯は Issue #1369
eval-results/

# 旧ロードマップ（docs/roadmap/）を移した凍結記録。書き換えないので整形もしない。
# 当初は並行更新との衝突を避けるために docs/roadmap/ を対象外にしていた（Issue #551）。
# 移設の経緯は Issue #1458
docs/archive/roadmap/
